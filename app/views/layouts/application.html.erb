<%# app/views/layouts/application.html.erb %>
<!DOCTYPE html>
<html>
  <head>

    <title><%= content_for(:title) || "Teakeypr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!--Altered for the to work with `rails assets:precomile` correctly -->
    <link rel="icon" type="image/png" href="<%= asset_path 'icon.png' %>">
    <link rel="icon" type="image/svg+xml" href="<%= asset_path 'icon.svg' %>">
    <%= favicon_link_tag 'favicon.ico' %>


    
    <!--ADDED for Select2 through CDN options-->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css", rel: "stylesheet" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js", defer: true %>


    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'layouts/navbar' %>

    <% if notice %>
      <p class="notice-message"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert-message"><%= alert %></p>
    <% end %>    
    
    <%= yield %>
  </body>
</html>
